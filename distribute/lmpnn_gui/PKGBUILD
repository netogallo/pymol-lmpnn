_realname=lmpnn-gui
pkgbase=mingw-w64-python-${_realname}
pkgname=("${MINGW_PACKAGE_PREFIX}-python-${_realname}")
pkgver="${LMPNN_GUI_VERSION}"
pkgrel="${LMPNN_GUI_RELEASE}"
pkgdesc="A GUI to use the ligand-mpnn model"
arch=('any')
mingw_arch=('mingw64')
license=('GPLv3')
makedepends=("${MINGW_PACKAGE_PREFIX}-python-poetry")
depends=(
  "${MINGW_PACKAGE_PREFIX}-python-pyqt5"
  "${MINGW_PACKAGE_PREFIX}-pyqt5-sip"
  "${MINGW_PACKAGE_PREFIX}-qt5-declarative")
source=('lmpnn_gui.src.tar.gz')
md5sums=('SKIP')

build() {
  BUILDDIR="python-build-mingw-w64"
  cp -r "lmpnn_gui" "$BUILDDIR"
  cd "$BUILDDIR" && "/mingw64/bin/poetry" build -f wheel
}

package() {
  cd "${srcdir}/python-build-mingw-w64"
  MSYS2_ARG_CONV_EXCL="--prefix=" \
    "/mingw64/bin/python" -m installer --prefix="/mingw64" \
    --destdir="${pkgdir}" dist/*.whl
}

